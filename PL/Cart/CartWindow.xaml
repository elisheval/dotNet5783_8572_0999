<Window x:Class="PL.Cart.CartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Cart"
        mc:Ignorable="d"
        Title="CartWindow" Height="450" Width="800"
     DataContext = "{Binding RelativeSource={RelativeSource Self}}" >
    <StackPanel>
        <Button Content="confirm order" Click="NavigateToConfirmOrder"></Button>
        <Button Content="back to product catalog" Click="NavigateToProductCatalog"></Button>
        <Label Content="{Binding message}"></Label>
        <ListView  ItemsSource="{Binding OIList}" >
            <ListView.View>
                <GridView x:Name="GridView1">
                    <GridViewColumn x:Name="idColumn" DisplayMemberBinding= "{Binding ProductId}" 
                                Header="product_id" Width="100"/>
                    <GridViewColumn DisplayMemberBinding= "{Binding Price}" 
                                Header="price" Width="100"/>
                    <GridViewColumn DisplayMemberBinding= "{Binding orderItemsList.ProductName}" 
                                Header="product_name" Width="100"/>
                    <GridViewColumn DisplayMemberBinding= "{Binding AmountInCart}" 
                                Header="amount_in_cart" Width="100"/>
                    <GridViewColumn DisplayMemberBinding= "{Binding TotalPriceForItem}" 
                                Header="total_price_for_item" Width="100"/>
                    <GridViewColumn Width="100" Header="remove item">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button
                                    Click="BtnRemoveItem"
                       Content="remove"
                       Margin="0"
                       VerticalAlignment="Center"
                        />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Width="100" Header="add amount">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                <Button
                       Click="btnAddAmount"
                       Content="+"
                       Margin="0"
                       VerticalAlignment="Center"
                        />
                            <Button
                       Click="btnSubtractAmount"
                       Content="-"
                       Margin="0"
                       VerticalAlignment="Center"
                        />
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
        
    
</Window>
